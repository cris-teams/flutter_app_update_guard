name: example_project
description: An example project with outdated dependencies to test flutter_app_update_guard.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  dio: ^4.0.0
  path: ^1.8.0

dev_dependencies:
  flutter_app_update_guard:
    path: ../
